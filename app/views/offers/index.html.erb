<h1>Lista de Ofertas</h1>

<%= search_form_for @q do |f| %>
  <%= f.search_field :title_cont, placeholder: "Buscar por título" %>
  <%= f.submit "Buscar" %>
<% end %>

<ul>
  <% @offers.each do |offer| %>
    <li>
      <h3><%= offer.title %></h3>
      <p><%= offer.description %></p>
    </li>
  <% end %>
</ul>